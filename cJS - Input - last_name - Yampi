function () {
  var nome = document.querySelector('input#name').value.trim();
 
  // Dividir o nome em palavras
  var palavras = nome.split(/\s+/);
 
  // Retornar a última palavra
  return palavras[palavras.length - 1];
}
